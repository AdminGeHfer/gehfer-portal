[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Uncomment if you need to handle SPA routing
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200